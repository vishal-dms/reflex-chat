version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip install -r requirements.txt
      - pip install reflex
run:
  command: reflex run --env prod --backend-only
  network:
    port: 3000
